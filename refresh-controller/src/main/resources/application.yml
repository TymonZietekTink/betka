server:
  port: 8183
spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/betka
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
