server:
  port: 8183
spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/betka
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
